diff --git a/planning/documentation-action-log.md b/planning/documentation-action-log.md
index ff73ebd..517d791 100644
--- a/planning/documentation-action-log.md
+++ b/planning/documentation-action-log.md
@@ -1768,3 +1768,106 @@ Follow-ups:
 Notes:
   The repo-structure memory is an orientation map, not proof of a complete current repository tree.
 
+### 2026-06-02 - Made review-diff-file mode the default archive review flow
+
+Date:
+  2026-06-02
+Action:
+  Added review-diff-file workflow documentation and made repo-stored review diff files the default archive review transfer.
+Type:
+  Archive workflow / command routing / Tampermonkey helper update
+Status:
+  applied
+Why:
+  Pasting large diffs into chat can turn the diff into an attached file and consume upload/file limits. The default archive workflow should create review diff files in the repository and let the chat read them from repo/branch/path instead.
+Changed files:
+  - planning/documentation/review-diff-file-workflow.md
+  - planning/replacement-file-generation-guide.md
+  - planning/planning-use-case-map.md
+  - planning/workstreams/tampermonkey-command-projection-plan.md
+  - tools/tampermonkey/chat-command-palette.user.js
+  - tools/tampermonkey/README.md
+  - tools/tampermonkey/IMPLEMENTATION-NOTES.md
+  - planning/workstreams/command-system-and-tampermonkey-goal-map.md
+  - planning/documentation-action-log.md
+Updates:
+  - Added owner workflow for review-diff-file mode.
+  - Made review-diff-file mode the default post-apply review transfer for archives.
+  - Added standard paths `_ai-review-diffs/last-archive.diff` and `_ai-review-diffs/last-archive-summary.md`.
+  - Added rule that review-diff-only commit/push may include only review artifact files.
+  - Added rule that real archive files must not be committed or pushed before diff review.
+  - Updated `давай архив` command reminders and helper body to use review-diff-file mode.
+Not changed:
+  - No helper-owned compose buffer implemented.
+  - No selected-command stack or cleanup UI implemented.
+  - No preview/search/external profile loading added.
+  - No Generic Action Overview implementation added.
+Follow-ups:
+  Apply this archive using review-diff-file mode and ask the chat to review the repo-stored diff before committing real files.
+Notes:
+  Clipboard/pasted diff transfer remains a fallback only when repo-stored review diff is not possible.
+
+### 2026-06-02 - Fixed review-diff-file command snippets
+
+Date:
+  2026-06-02
+Action:
+  Fixed review-diff-file workflow commands after the first live run exposed command-template problems.
+Type:
+  Archive workflow bug fix / command template correction
+Status:
+  applied
+Why:
+  The first live review-diff-file run showed that whitespace-checking `_ai-review-diffs/*.diff` artifacts can fail because diff files intentionally preserve whitespace from real file changes. It also showed the command templates should consistently use `git --no-pager diff` for diff/stat/name-only operations and should not rely on raw `git diff` forms.
+Changed files:
+  - planning/documentation/review-diff-file-workflow.md
+  - planning/replacement-file-generation-guide.md
+  - tools/tampermonkey/IMPLEMENTATION-NOTES.md
+  - planning/workstreams/command-system-and-tampermonkey-goal-map.md
+  - planning/documentation-action-log.md
+Updates:
+  - Fixed the broken inline path in implementation notes.
+  - Documented that review-diff-only commit commands must not run whitespace checks on `.diff` artifacts.
+  - Documented that diff/stat/name-only operations in the review-diff-file flow should use `git --no-pager diff`.
+  - Updated living Goal Map to TM-9.
+Not changed:
+  - No userscript behavior changed.
+  - No command semantics changed outside review-diff-file workflow corrections.
+  - No helper-owned buffer/stack/cleanup UI implemented.
+Follow-ups:
+  Regenerate and push the review diff file using the corrected commands, then review the repo-stored diff again before committing real files.
+Notes:
+  Whitespace checks should run on real staged files before the real commit, not on the stored `.diff` review artifact.
+
+### 2026-06-02 - Switched review-diff-file flow to one command and one artifact
+
+Date:
+  2026-06-02
+Action:
+  Updated review-diff-file archive workflow so the apply command itself commits and pushes only the single review diff artifact.
+Type:
+  Archive workflow correction / command template correction
+Status:
+  applied
+Why:
+  The review-diff-file workflow should not require a separate second command for committing review artifacts. The apply command should create the review diff and then commit/push only `_ai-review-diffs/last-archive.diff`, while real archive files remain local and uncommitted until review approval.
+Changed files:
+  - planning/documentation/review-diff-file-workflow.md
+  - planning/replacement-file-generation-guide.md
+  - tools/tampermonkey/IMPLEMENTATION-NOTES.md
+  - planning/workstreams/command-system-and-tampermonkey-goal-map.md
+  - planning/documentation-action-log.md
+Updates:
+  - Made `_ai-review-diffs/last-archive.diff` the only committed review artifact by default.
+  - Removed the default separate review-diff commit/push command concept.
+  - Removed default extra review artifact commit/push from the workflow.
+  - Updated implementation notes and living Goal Map to reflect one-command review-diff-file flow.
+Not changed:
+  - No userscript behavior changed.
+  - No command semantics changed outside review-diff-file workflow corrections.
+  - No helper-owned buffer/stack/cleanup UI implemented.
+Follow-ups:
+  Regenerate and push the review diff file using the one-command flow, then review the repo-stored diff again before committing real files.
+Notes:
+  Real archive files must still not be committed or pushed before diff review approval.
+
diff --git a/planning/documentation/review-diff-file-workflow.md b/planning/documentation/review-diff-file-workflow.md
new file mode 100644
index 0000000..2a75cdf
--- /dev/null
+++ b/planning/documentation/review-diff-file-workflow.md
@@ -0,0 +1,168 @@
+# Review Diff File Workflow
+
+Status: active archive review workflow  
+Scope: default repo-stored review diff file flow for replacement archive/package application
+
+Use with:
+
+```text
+planning/replacement-file-generation-guide.md
+planning/planning-use-case-map.md
+```
+
+## 1. Purpose
+
+This workflow defines how archive review should work when a replacement archive is applied locally.
+
+Default mode:
+
+```text
+review-diff-file mode
+```
+
+Instead of pasting a large diff into chat, the apply command creates and pushes a repository-stored review artifact:
+
+```text
+_ai-review-diffs/last-archive.diff
+```
+
+The assistant then reviews that repo-stored diff before giving commands to commit real archive files.
+
+## 2. Core Rule
+
+```text
+The apply command may commit and push only `_ai-review-diffs/last-archive.diff`.
+Real archive files must remain local and uncommitted until the review diff is approved.
+```
+
+Default committed review artifact:
+
+```text
+_ai-review-diffs/last-archive.diff
+```
+
+Do not create or commit extra review artifacts by default.
+
+## 3. Required Apply Flow
+
+The apply command should:
+
+```text
+1. pull the current branch;
+2. apply the archive replacement files;
+3. make expected new files visible with `$newFiles` and `git add -N`;
+4. create `_ai-review-diffs/last-archive.diff` using `git --no-pager diff --no-color --output=...`;
+5. stage only `_ai-review-diffs/last-archive.diff`;
+6. verify no other file is staged;
+7. commit and push only the review diff file;
+8. tell the user to ask the chat to review the diff file.
+```
+
+## 4. New File Visibility
+
+Untracked new files do not appear in `git diff` by default.
+
+Every archive command that expects new files must include:
+
+```powershell
+$newFiles = @(
+  "planning/documentation/review-diff-file-workflow.md"
+)
+
+foreach ($file in $newFiles) {
+  if (Test-Path $file) {
+    git add -N -- $file
+  } else {
+    throw "Missing expected new file: $file"
+  }
+}
+```
+
+Use `$newFiles = @()` when the archive does not add files.
+
+## 5. Diff Command Rules
+
+Use no-pager diff commands:
+
+```powershell
+git --no-pager diff --stat -- $files
+git --no-pager diff --no-color --output="$ReviewDiff" -- $files
+git --no-pager diff --cached --name-only
+```
+
+Do not use:
+
+```text
+- raw `git diff --stat -- $files`;
+- raw `git diff --cached --name-only`;
+- paged diff output;
+- full diff printed to terminal.
+```
+
+## 6. Review Artifact Commit Rules
+
+The review artifact commit may stage only:
+
+```text
+_ai-review-diffs/last-archive.diff
+```
+
+Required guard:
+
+```powershell
+$ReviewDiff = "_ai-review-diffs/last-archive.diff"
+$reviewFiles = @($ReviewDiff)
+
+git restore --staged -- .
+git add -- $ReviewDiff
+
+$staged = @(git --no-pager diff --cached --name-only)
+foreach ($f in $staged) {
+  if ($reviewFiles -notcontains $f) {
+    throw "Unexpected staged file. Refusing review-diff commit: $f"
+  }
+}
+
+if ($staged -notcontains $ReviewDiff) {
+  throw "Expected review diff file is not staged: $ReviewDiff"
+}
+```
+
+Do not run `git diff --cached --check` on review `.diff` artifacts. The `.diff` file intentionally preserves whitespace from the real diff.
+
+Run whitespace checks on real files before the real commit, after the review diff is approved.
+
+## 7. Chat Review Command
+
+After the apply command pushes the review diff, tell the chat:
+
+```text
+проверь review diff file
+Repo: <owner/repo>
+Branch: <branch>
+Path: _ai-review-diffs/last-archive.diff
+```
+
+The chat should read the diff from the repo and check:
+
+```text
+- expected files are present;
+- expected new files appear;
+- no unrelated files are included;
+- package intent matches the diff;
+- no old conflicting workflow text remains;
+- real files are not committed yet.
+```
+
+## 8. Approval Boundary
+
+Before approval:
+
+```text
+- do not commit real archive files;
+- do not push real archive files;
+- do not use `git add .`;
+- do not treat a review artifact commit as proof the real archive has landed.
+```
+
+After approval, the assistant may provide scoped real-file commit commands.
diff --git a/planning/planning-use-case-map.md b/planning/planning-use-case-map.md
index 166d718..918f036 100644
--- a/planning/planning-use-case-map.md
+++ b/planning/planning-use-case-map.md
@@ -1,6 +1,6 @@
 # Planning Use Case Map
 
-Status: current root use-case map / action-to-doc-flow router  
+Status: current root use-case map / action-to-doc-flow router
 Scope: maps user-visible actions and commands to planning docs, workflows, templates, sources and permission boundaries
 
 ## 1. Purpose
@@ -230,9 +230,10 @@ When the user asks for an archive/package output, the chat must read:
 
 ```text
 planning/replacement-file-generation-guide.md
+planning/documentation/review-diff-file-workflow.md
 ```
 
-Replacement package output must contain complete replacement/add files under `replacement-files/<repo-relative-path>`, plus `MANIFEST.md` and `APPLY.md`. Do not emit patch scripts or diff-only packages unless the user explicitly asks for patch proposal mode.
+Replacement package output must contain complete replacement/add files under `replacement-files/<repo-relative-path>`, plus `MANIFEST.md` and `APPLY.md`. Default post-apply review transfer is review-diff-file mode: create `_ai-review-diffs/last-archive.diff` and `_ai-review-diffs/last-archive-summary.md`, then give a separate command to commit/push only those review diff files. Do not emit patch scripts or diff-only packages unless the user explicitly asks for patch proposal mode.
 
 ## 7. Source Model
 
@@ -333,6 +334,16 @@ planning/repo-structure-memory.md
 The command reconstructs known repository root areas, documentation/source-of-truth layers, active workstream files and next files to read. It does not grant edit, archive/package, commit or push permission.
 
 
+Review Diff File / archive review transfer behavior is owned by:
+
+```text
+planning/documentation/review-diff-file-workflow.md
+```
+
+Default archive output now uses repo-stored review diff files instead of pasted giant diffs. Review-diff-only commits may push `_ai-review-diffs/last-archive.diff` and `_ai-review-diffs/last-archive-summary.md`; real archive files must not be committed or pushed before diff review.
+
+
+
 Response block and response modifier commands such as `кп`, `саммари`, `кц`, `карта цели кратко`, `goal map brief`, `крит`, `критически`, `critical review`, `план файл-обновление`, legacy `итог` and `отличия драфта` are routed in this file, but their behavior is owned by `reviewable-agent-output-and-commands-workflow.md`.
 
 Goal Map maintenance commands such as `синх карта` are routed in this file, but their behavior is owned by `planning/goal-map-principles-workflow-template.md`.
@@ -398,6 +409,7 @@ This root map remains the concrete Enman route table.
 | `отличия драфта`, `draft diff` | Show how the active draft changed from the previous version. | Ask for the draft target if no active draft is clear. | Reuse / targeted. | Conversation / canvas / draft sources if changed. | `Отличия от предыдущего драфта` plus updated draft when requested. |
 | `обс`, `перепроверь обсуждение`, `context recheck` | Re-check relevant prior discussion, accepted decisions and constraints before answering. | Re-check available prior discussion or say what context is unavailable. | Reuse / targeted; full only if task requires it. | Conversation plus sources only if needed. | Context/discussion recheck result; combine with Level 1/2/3 by task breadth. |
 | `вспомни структуру репо`, `структура репо`, `вспомни репо`, `слои репо`, `где что лежит`, `repo structure`, `repo layers` | Reconstruct repo structure and source-of-truth layers for the active task before planning/editing. | Provide current known repo structure or ask for repo/archive/tree if unavailable. | Targeted; full when structure is stale/unknown or task touches unknown layers. | GitHub/repo tree/archive plus root planning docs and active workstream docs as needed. | Repo Structure Brief: root areas, documentation layers, source-of-truth chain, active workstream files, code/tooling/example areas, known vs uncertain, files to read next. |
+| `проверь review diff file`, `проверь diff file`, `review diff file` | Read the standard repo-stored review diff for the active archive context and review it before real files are committed. | Ask for repo/branch/path if not clear; do not ask for pasted full diff when repo access is available. | Targeted. | GitHub/repo path `_ai-review-diffs/last-archive.diff` plus summary when needed. | Diff review verdict; if OK, scoped real commit/push commands for intended files only. |
 | `перепроверь`, `recheck` | Recheck active answer/draft/source coverage. | Recheck last answer or ask target. | Targeted / full depending risk. | Conversation plus relevant sources. | Findings and corrections/no-change result. |
 | `учти файл X` | Incorporate explicit new source into active work. | Use X for the requested new answer. | Targeted. | Uploaded/named file. | Updated answer/draft with Source Delta. |
 | `без изм`, `б изм`, `no ch` | Reuse recent context and avoid broad re-audit. | Weak without prior context; ask what state is unchanged if needed. | Reuse / targeted. | Previous context plus targeted reads. | Answer/update with minimal checks. |
diff --git a/planning/replacement-file-generation-guide.md b/planning/replacement-file-generation-guide.md
index b2e8825..66515c9 100644
--- a/planning/replacement-file-generation-guide.md
+++ b/planning/replacement-file-generation-guide.md
@@ -38,7 +38,8 @@ Replacement archive/package mode means:
 - repository-relative paths under replacement-files/;
 - MANIFEST.md;
 - APPLY.md;
-- no patch scripts, diff-only files or generated apply scripts.
+- no patch scripts, diff-only files or generated apply scripts;
+- default post-apply review transfer through `_ai-review-diffs/last-archive.diff`.
 ```
 
 If complete replacement files cannot be produced safely, stop and say so. Do not silently switch to patch-script mode.
@@ -70,6 +71,10 @@ Use this guide when the user asks to create an archive or replacement package fo
 - Do not include unrelated implementation changes.
 - Keep archive scope focused, but do not artificially split one coherent accepted update into many tiny archives.
 - Do not include patch scripts or script-based patch applicators in replacement archive mode.
+- Use review-diff-file mode as the default post-apply review transfer.
+- The apply command itself must create, commit and push only `_ai-review-diffs/last-archive.diff`.
+- Do not create or commit extra review artifacts by default.
+- Do not commit or push real archive replacement files before diff review.
 - If a coherent update has safe files and large/risky files, split by delivery safety instead of blocking the safe archive.
 ```
 
@@ -213,42 +218,66 @@ Preserved
   = unrelated information was not lost, removed or overwritten.
 ```
 
-Every APPLY.md should tell the user to review both application and preservation before committing.
+Every APPLY.md and assistant archive response should tell the user to review both application and preservation before committing real archive files.
 
-Include commands like:
+Default post-apply review transfer is:
 
-```powershell
-git status --short -- $files
-git diff --stat -- $files
-git --no-pager diff --no-color --output="$diffFile" -- $files
-
-$diffText = [System.IO.File]::ReadAllText($diffFile, [System.Text.Encoding]::UTF8)
-Set-Clipboard -Value $diffText
+```text
+review-diff-file mode
 ```
 
-Use the full command shape in §7B for package-specific `$files`, `$newFiles`, `$pkgName` and `$diffFile`. Do not print the full diff to the terminal.
+Default review artifact path:
+
+```text
+_ai-review-diffs/last-archive.diff
+```
 
 The post-apply review should check:
 
 ```text
-- only intended files changed;
+- only intended real files changed;
+- all expected new real files appear in the diff, using `git add -N` when needed;
 - diff matches package intent;
 - no unrelated sections, register entries, commands, examples, routing rows or source-of-truth rules were removed;
 - shared-state files such as registers preserve existing entries unless removal was explicit;
-- commit commands add only intended files and never use `git add .`.
+- real commit commands add only intended real files and never use `git add .`;
+- review-diff commit steps stage only `_ai-review-diffs/last-archive.diff`.
 ```
 
-If the user asks `проверь` after applying a replacement archive, treat it as this post-apply preservation check, not only as a file-name/status check.
+If the user asks `проверь` after applying a replacement archive, treat it as this post-apply preservation check. If a review diff file has been pushed, read `_ai-review-diffs/last-archive.diff` from the active repo/branch instead of asking for a pasted diff.
 
-## 7B. Diff Capture And Clipboard Commands
+## 7B. Review Diff File Mode (Default)
 
-When the assistant provides a replacement archive, the chat response should include ready-to-run PowerShell commands for post-apply review, not only instructions hidden inside `APPLY.md`.
+When the assistant provides a replacement archive, the chat response should include ready-to-run PowerShell commands that apply the archive, create a repo-stored review diff and commit/push only that review artifact.
 
-The command block should avoid paged terminal diff output and should avoid PowerShell pipeline encoding problems with Cyrillic text. It must print only `git status --short`, `git diff --stat` and short `Write-Host` messages. The full diff must be saved to a file and copied from that file to the clipboard without printing it to the terminal.
+Default mode:
 
-It must also make expected new files visible in the review diff. Untracked files are not shown by `git diff` by default.
+```text
+review-diff-file mode
+```
 
-Required shape:
+All assistant-provided diff commands in this mode must use `git --no-pager diff`. The full diff must be written to a file with `--output`; it must not be printed to the terminal or opened in a pager.
+
+Required response shape:
+
+```text
+1. Apply archive replacement files.
+2. Make expected new files visible using `$newFiles` and `git add -N`.
+3. Create `_ai-review-diffs/last-archive.diff` with `git --no-pager diff --no-color --output=...`.
+4. In the same apply command, commit/push only `_ai-review-diffs/last-archive.diff`.
+5. Tell chat to review `_ai-review-diffs/last-archive.diff`.
+6. Keep real archive replacement files local and uncommitted until review approval.
+```
+
+The default committed review artifact is exactly one file:
+
+```text
+_ai-review-diffs/last-archive.diff
+```
+
+Do not create or commit extra review artifacts by default.
+
+Required apply-command shape:
 
 ```powershell
 $files = @(
@@ -267,70 +296,118 @@ foreach ($file in $newFiles) {
   if (Test-Path $file) {
     git add -N -- $file
   } else {
-    Write-Host "Missing expected new file: $file"
+    throw "Missing expected new file: $file"
   }
 }
 
-$pkgName = "real-package-name-without-angle-brackets"
-$diffFile = Join-Path (Get-Location) "$pkgName.diff"
+$ReviewDir = "_ai-review-diffs"
+$ReviewDiff = "$ReviewDir/last-archive.diff"
+$reviewFiles = @($ReviewDiff)
+
+New-Item -ItemType Directory -Force $ReviewDir | Out-Null
 
 git status --short -- $files
-git diff --stat -- $files
+git --no-pager diff --stat -- $files
+git --no-pager diff --no-color --output="$ReviewDiff" -- $files
 
-git --no-pager diff --no-color --output="$diffFile" -- $files
+$size = (Get-Item $ReviewDiff).Length
+if ($size -eq 0) {
+  throw "Review diff file is 0 bytes. Refusing review-diff commit."
+}
 
-$diffText = [System.IO.File]::ReadAllText($diffFile, [System.Text.Encoding]::UTF8)
-Set-Clipboard -Value $diffText
+git restore --staged -- .
+git add -- $ReviewDiff
 
-Write-Host "Diff saved to: $diffFile"
-Write-Host "Diff copied to clipboard. Paste it into chat for review before committing."
+$staged = @(git --no-pager diff --cached --name-only)
+foreach ($f in $staged) {
+  if ($reviewFiles -notcontains $f) {
+    throw "Unexpected staged file. Refusing review-diff commit: $f"
+  }
+}
+
+if ($staged -notcontains $ReviewDiff) {
+  throw "Expected review diff file is not staged: $ReviewDiff"
+}
+
+# Do not run `git diff --cached --check` on review `.diff` artifacts.
+# Review artifacts preserve whitespace from the real diff.
+git commit -m "review: update last archive diff"
+git push origin <branch>
 ```
 
 Rules:
 
 ```text
-- Use a real package name in `$pkgName`; do not leave placeholders such as `<ARCHIVE_NAME>` in runnable commands.
-- `$files` must include all expected changed and added files.
-- `$newFiles` must include every expected added file; use `$newFiles = @()` when the package does not add files.
+- Use a real package name and branch in runnable commands; do not leave placeholders in final user-facing commands.
+- `$files` must include all expected changed and added real files.
+- `$newFiles` must include every expected added real file; use `$newFiles = @()` when the package does not add files.
 - Run `git add -N -- <new-file>` before generating the review diff for packages that add files.
-- `git add -N` is used only to make untracked files visible in `git diff`; the final commit command must still explicitly stage only intended files.
+- `git add -N` is used only to make untracked files visible in `git diff`.
 - If an expected new file is missing, treat that as a review blocker until resolved.
-- Use `git --no-pager diff --no-color --output="$diffFile" -- $files` to write the diff file.
-- Copy the saved diff with `[System.IO.File]::ReadAllText($diffFile, [System.Text.Encoding]::UTF8)` and `Set-Clipboard -Value $diffText`.
-- Do not rely on `git diff | Tee-Object | Set-Clipboard`, raw `git diff`, or `Get-Content ... | Set-Clipboard` for reviewable diff transfer when non-ASCII text may be present.
-- Do not ask the user to manually scroll through paged diff output.
-- Do not print the full diff to the terminal; only status/stat and short status messages may be printed.
+- Use `git --no-pager diff --stat -- $files` for stat output.
+- Use `git --no-pager diff --no-color --output="$ReviewDiff" -- $files` to write the review diff file.
+- Do not copy full diff to clipboard by default.
+- Do not ask the user to paste a giant diff into chat by default.
+- Do not print the full diff to terminal.
+- The assistant should later read `_ai-review-diffs/last-archive.diff` from the repo/branch.
+- Real archive files must not be committed or pushed before diff review approval.
+- Review-diff commit steps must not stage any file outside `_ai-review-diffs/last-archive.diff`.
+- Do not run whitespace checks on `_ai-review-diffs/*.diff`; run whitespace checks on real files before real commit.
 ```
 
-If the pasted diff shows mojibake or suspicious broken Cyrillic, do not conclude from the diff alone that the repository file is corrupted. Ask for or provide a suspect-file content copy command.
+## 7C. Legacy Clipboard Diff Commands (Fallback Only)
+
+Clipboard diff transfer is no longer the default archive review mode.
+
+Use it only when:
+
+```text
+- repo-stored review diff is impossible;
+- the user explicitly asks to paste/copy the diff;
+- target repo policy forbids committing review artifacts;
+- connector/repo access is unavailable for reading review diff files.
+```
 
 Fallback command shape:
 
 ```powershell
-$suspectFiles = @(
-  "path/to/suspect-file-1.md",
-  "path/to/suspect-file-2.md"
+$files = @(
+  "path/to/changed-file-1.md",
+  "path/to/changed-file-2.md",
+  "path/to/new-file.md"
 )
 
-$pkgName = "real-package-name-without-angle-brackets"
-$suspectDump = Join-Path (Get-Location) "$pkgName.suspect-files.txt"
+$newFiles = @(
+  "path/to/new-file.md"
+)
 
-$bundle = foreach ($file in $suspectFiles) {
-  "===== $file ====="
-  Get-Content -Path $file -Raw -Encoding UTF8
-  ""
+foreach ($file in $newFiles) {
+  if (Test-Path $file) {
+    git add -N -- $file
+  } else {
+    throw "Missing expected new file: $file"
+  }
 }
 
-$bundle -join "`r`n" | Set-Content -Path $suspectDump -Encoding UTF8
+$pkgName = "real-package-name-without-angle-brackets"
+$diffFile = Join-Path (Get-Location) "$pkgName.diff"
 
-$suspectText = [System.IO.File]::ReadAllText($suspectDump, [System.Text.Encoding]::UTF8)
-Set-Clipboard -Value $suspectText
+git status --short -- $files
+git --no-pager diff --stat -- $files
+git --no-pager diff --no-color --output="$diffFile" -- $files
 
-Write-Host "Suspect file contents saved to: $suspectDump"
-Write-Host "Suspect file contents copied to clipboard. Paste it into chat."
+$diffText = [System.IO.File]::ReadAllText($diffFile, [System.Text.Encoding]::UTF8)
+Set-Clipboard -Value $diffText
+
+Write-Host "Diff saved to: $diffFile"
+Write-Host "Diff copied to clipboard. Paste it into chat for review before committing."
 ```
 
-## 7C. Replacement Archive Conversation Review Loop
+Do not rely on `git diff | Tee-Object | Set-Clipboard`, raw `git diff`, or `Get-Content ... | Set-Clipboard` for reviewable diff transfer when non-ASCII text may be present.
+
+If the pasted diff shows mojibake or suspicious broken Cyrillic, do not conclude from the diff alone that the repository file is corrupted. Ask for or provide a suspect-file content copy command.
+
+## 7D. Replacement Archive Conversation Review Loop
 
 Replacement archive work is a review loop between the assistant and the user.
 
@@ -342,37 +419,37 @@ Use this loop when the assistant creates a replacement archive and the user appl
    - archive link;
    - intended changed files;
    - intended new files, if any;
-   - PowerShell apply commands;
-   - full diff capture commands from §7B;
-   - mojibake/suspect-file fallback commands when relevant;
-   - scoped commit commands, clearly marked as after-review only.
-3. User applies the archive locally and sends the copied diff.
-4. Assistant reviews the diff before telling the user to commit.
-5. If the diff is OK, assistant gives scoped `git add`, `git commit` and `git push` commands.
-6. User commits and pushes.
-7. When user says `проверь`, assistant checks the remote branch/files and confirms what landed.
+   - PowerShell apply command that creates and pushes the review diff file;
+   - scoped real commit commands only after review approval.
+3. User runs the apply command locally.
+4. The apply command commits/pushes only `_ai-review-diffs/last-archive.diff`.
+5. User asks the assistant to review the repo-stored review diff file.
+6. Assistant reviews the diff before telling the user to commit real archive files.
+7. If the diff is OK, assistant gives scoped `git add`, `git commit` and `git push` commands for real files.
+8. User commits and pushes real files.
+9. When user says `проверь`, assistant checks the remote branch/files and confirms what landed.
 ```
 
 Diff review must check:
 
 ```text
-- only intended files appear in the scoped diff;
-- all expected new files appear in the diff, using `git add -N` when needed;
+- only intended real files appear in the scoped diff;
+- all expected new real files appear in the diff, using `git add -N` when needed;
 - no expected file is missing;
 - no unrelated sections, register entries, examples, routing rows or source-of-truth rules were removed;
 - package intent matches the diff;
 - service files such as MANIFEST.md and APPLY.md were not added to the repository unless they are intended repository files;
-- final commit commands stage only intended files and do not use `git add .`.
+- final real commit commands stage only intended real files and do not use `git add .`.
 ```
 
-If the user's diff is incomplete:
+If the review diff file is incomplete:
 
 ```text
-- do not approve commit yet;
+- do not approve real commit yet;
 - explain what is missing;
-- provide the exact command needed to regenerate the full diff;
+- provide the exact command needed to regenerate the review diff file;
 - for missing added files, provide the `$newFiles` / `git add -N` command from §7B;
-- for mojibake, provide the suspect-file content copy command from §7B.
+- for mojibake in fallback pasted diff mode, provide the suspect-file content copy command from §7C.
 ```
 
 If the diff is correct:
@@ -380,13 +457,13 @@ If the diff is correct:
 ```text
 - state that the diff is complete enough for review;
 - summarize what was checked;
-- give only scoped commit commands for the intended files;
+- give only scoped real commit commands for the intended files;
 - remind not to use `git add .` when unrelated local changes may exist.
 ```
 
-This loop is not a new output mode. It is the review procedure for replacement archive/package output mode.
+This loop is not a new output mode. It is the default review procedure for replacement archive/package output mode.
 
-## 7D. Replacement Archive Batch Scope Rule
+## 7E. Replacement Archive Batch Scope Rule
 
 Do not artificially split a coherent accepted update into many tiny archives.
 
@@ -408,11 +485,11 @@ Prefer one coherent archive when:
 - splitting would create extra manual apply/diff/commit/recheck cycles without improving safety.
 ```
 
-Safety comes from complete replacement files, scoped file lists, full diff capture, preservation checks and scoped commit commands, not from making every archive artificially tiny.
+Safety comes from complete replacement files, scoped file lists, review diff files, preservation checks and scoped commit commands, not from making every archive artificially tiny.
 
 Do not bundle unrelated work just to reduce archive count.
 
-## 7E. Hybrid Archive / Script Delivery Rule
+## 7F. Hybrid Archive / Script Delivery Rule
 
 Use hybrid delivery when one coherent accepted update contains both safe complete-replacement files and large/shared files whose complete replacement is unsafe and therefore need targeted script edits.
 
@@ -426,7 +503,7 @@ Separate Local Targeted Script Edit file(s):
   - one `.ps1` per large/shared file that needs targeted writes.
 
 Final review:
-  - one combined scoped diff command may include all affected files.
+  - one combined scoped review diff file may include all affected files.
 ```
 
 Rules:
@@ -548,7 +625,7 @@ Every final response with an archive should state:
 - any blocking questions.
 ```
 
-When the archive is intended for one local bulk commit, the final response should also include a suggested `git add` and `git commit` command.
+When the archive is intended for one local bulk commit, the final response should include real `git add`, `git commit` and `git push` commands only after the review diff file has been reviewed and approved.
 
 ## 11. Status Reconciliation Rule
 
@@ -571,6 +648,8 @@ Do not leave docs saying `planned` when current repo evidence shows `implemented
 - Do not generate patch scripts when replacement files are expected.
 - Do not directly change GitHub when the user asked for an archive.
 - Do not provide archive apply instructions without a pull/current-state step first.
+- Do not ask for pasted giant diffs by default; use review-diff-file mode.
+- Do not run `git diff --cached --check` on review `.diff` artifacts.
 - Do not tell the user to extract a package-layout archive directly into the repo root as the apply step.
 - Do not let one large/risky file block safe archive delivery for other files.
 - Do not choose script mode only because a file is large.
diff --git a/planning/workstreams/command-system-and-tampermonkey-goal-map.md b/planning/workstreams/command-system-and-tampermonkey-goal-map.md
index 3b007a4..96ffbf9 100644
--- a/planning/workstreams/command-system-and-tampermonkey-goal-map.md
+++ b/planning/workstreams/command-system-and-tampermonkey-goal-map.md
@@ -21,13 +21,13 @@ planning/documentation/examples/README.md
 ## 0. Current Snapshot
 
 Last updated:
-  2026-06-02 / after TM-7 repo-structure command and deferred-goals owner
+  2026-06-02 / after TM-10 one-command review-diff-file flow
 
 Current goal:
   Build a transferable command system and Tampermonkey prompt helper for long-running chats across projects.
 
 Current focus:
-  TM-7 — Repo-structure command and deferred-goals owner.
+  TM-10 — One-command review-diff-file flow.
 
 Active slice:
   SL-6 — Tampermonkey Guided Prompt Helper
@@ -57,6 +57,10 @@ Already available:
   - Command row click no longer closes the widget.
   - Repo-structure orientation command is documented and available in the helper.
   - Deferred goals and far-future ideas owner file exists.
+  - Review-diff-file mode is the default archive review transfer.
+  - Review-diff-file commands no longer whitespace-check `.diff` artifacts.
+  - Review-diff-file command snippets use `git --no-pager diff` consistently.
+  - Review-diff-file flow now commits/pushes only one artifact file from the apply command.
 
 Latest completed:
   - Added root Goal Map owner/example.
@@ -78,12 +82,15 @@ Latest completed:
   - Recorded first `синх карта` command insertion smoke test.
   - Fixed command row click so it keeps the widget open.
   - Added repo-structure orientation command and deferred-goals owner file.
+  - Made review-diff-file mode the default archive review transfer.
+  - Fixed review-diff-file command snippets after first live run.
+  - Switched review-diff-file flow to one-command / one-file review artifact.
 
 Next action:
-  Continue TM-7:
-    apply/review this docs+helper update, then continue smoke testing without implementing deferred buffer/stack ideas.
+  Continue TM-10:
+    apply this flow-fix archive; the apply command should generate and push only `_ai-review-diffs/last-archive.diff`, then ask the chat to review again.
 Recommended next action:
-  Apply and review this repo-structure/deferred-goals archive, then retest the helper command list and continue smoke testing.
+  Apply and review this one-command review-diff-file archive before committing real files.
 
 Open decisions:
   - DEC-1: resolved — Generic Action Overview deferred; TM-0 selected.
@@ -973,12 +980,12 @@ Status:
 
 ## 9. Current Focus
 
-TM-7 — Repo-structure command and deferred-goals owner:
-  - new command `вспомни структуру репо` is routed and projected into the helper;
-  - new owner file `planning/repo-structure-memory.md` records known repo layers and orientation output;
-  - new owner file `planning/deferred-goals-and-ideas.md` stores deferred/far-future ideas;
-  - Tampermonkey buffer/selected-command-stack/cleanup ideas are recorded as far future, not implemented now;
-  - current near-term work remains smoke testing and small bug fixes;
+TM-10 — One-command review-diff-file flow:
+  - archive apply command should also commit/push the single review diff artifact;
+  - default committed review artifact is only `_ai-review-diffs/last-archive.diff`;
+  - no separate review-diff commit/push command is needed by default;
+  - no extra review artifact is committed/pushed by default;
+  - real archive files still must not be committed or pushed before diff review;
   - keep `planning/planning-use-case-map.md` and linked owner workflows as source of truth;
   - keep Generic Action Overview deferred.
 
@@ -987,15 +994,15 @@ Current active slice:
 
 ## 10. Next Action
 
-Retest the helper after the repo-structure/deferred-goals update:
+Apply this flow-fix archive using the one-command review-diff-file flow:
 
 ```text
-1. Confirm `вспомни структуру репо` appears in the command list.
-2. Click it and confirm its body inserts while the widget stays open.
-3. Confirm header click closes the widget.
-4. Continue scroll / MVP-1 / MVP-2 / empty-composer / existing-text / no-auto-send tests.
-5. Do not implement helper-owned compose buffer or selected-command stack yet.
+1. Apply replacement files.
+2. Regenerate `_ai-review-diffs/last-archive.diff`.
+3. Commit/push only `_ai-review-diffs/last-archive.diff`.
+4. Ask chat to review the repo-stored review diff again.
+5. Do not commit/push real archive files until review is approved.
 ```
 
 Recommended next action:
-  Finish smoke testing before adding preview, search, external profile loading or deferred buffer/stack features.
+  Confirm the regenerated review diff uses one command, one committed review artifact, no pager diff commands and no whitespace-checks on review `.diff` artifacts.
diff --git a/planning/workstreams/tampermonkey-command-projection-plan.md b/planning/workstreams/tampermonkey-command-projection-plan.md
index 945e97b..69c4d51 100644
--- a/planning/workstreams/tampermonkey-command-projection-plan.md
+++ b/planning/workstreams/tampermonkey-command-projection-plan.md
@@ -110,10 +110,11 @@ key_reminders:
   - Output-package mode, not archive read-source mode.
   - Use active approved scope; ask only blocking questions.
   - Follow replacement archive workflow and current package layout rules.
-  - Include ready-to-run apply/diff commands directly in chat.
-  - Save full diff to file and copy it with ReadAllText(... UTF8) + Set-Clipboard.
-  - Ask user to paste diff before commit.
-  - Never commit/push without diff review.
+  - Include ready-to-run apply commands directly in chat.
+  - Default to review-diff-file mode, not clipboard diff paste.
+  - Apply command creates `_ai-review-diffs/last-archive.diff` and summary.
+  - Give a separate command to commit/push only review diff files.
+  - Never commit/push real archive files before diff review.
 ```
 
 ### archive_source.use
@@ -243,7 +244,7 @@ command:
   давай архив
 
 command_family:
-  `давай архив` / `собери архив` / `replacement package`
+  `давай архив` / `собери архив` / `давай архив с дифф-файлом` / `replacement package`
 
 source_of_truth:
   Start from `planning/planning-use-case-map.md`.
@@ -258,10 +259,10 @@ key_reminders:
   - Output-package mode, not archive read-source mode.
   - Use active approved scope; ask only blocking questions.
   - Do not put apply commands only inside the archive.
-  - Give apply/diff commands in chat.
-  - Save full diff to file and copy it to clipboard.
-  - Ask user to paste diff before commit.
-  - Do not commit or push.
+  - Give apply commands and separate review-diff-only commit/push commands in chat.
+  - Default review uses `_ai-review-diffs/last-archive.diff`, not pasted clipboard diff.
+  - Commit/push only review diff files before review.
+  - Do not commit or push real archive files before diff review.
 
 user_target:
   <what archive/package should include>
diff --git a/tools/tampermonkey/IMPLEMENTATION-NOTES.md b/tools/tampermonkey/IMPLEMENTATION-NOTES.md
index a9b4a2b..55ac1a8 100644
--- a/tools/tampermonkey/IMPLEMENTATION-NOTES.md
+++ b/tools/tampermonkey/IMPLEMENTATION-NOTES.md
@@ -66,7 +66,9 @@ tools/tampermonkey/chat-command-palette.user.js
 - First userscript skeleton exists as `tools/tampermonkey/chat-command-palette.user.js`.
 - Command row click must not close the widget; only header click toggles open/closed.
 - Deferred and far-future goals live in `planning/deferred-goals-and-ideas.md`.
+- Archive review-diff-file workflow lives in `planning/documentation/review-diff-file-workflow.md`.
 - Repo-structure orientation command is available as `вспомни структуру репо`.
+- Default archive review transfer uses one repo-stored review diff file.
 ```
 
 ## 4. Implementation Principles
@@ -346,6 +348,30 @@ The chat produces a Repo Structure Brief and identifies files to read next befor
 ```
 
 
+### UC-10 — User creates archive with default review diff file
+
+Example:
+
+```text
+давай архив
+```
+
+Expected helper/chat behavior:
+
+```text
+- archive response still includes archive link and apply commands;
+- apply command creates `_ai-review-diffs/last-archive.diff`;
+- the same apply command commits/pushes only `_ai-review-diffs/last-archive.diff`;
+- real archive files are not committed or pushed before diff review.
+```
+
+Success condition:
+
+```text
+The chat can later read the review diff file from repo/branch/path without the user pasting a giant diff into chat.
+```
+
+
 ## 6. What Should Be Possible
 
 First userscript MVP should make this possible:
diff --git a/tools/tampermonkey/README.md b/tools/tampermonkey/README.md
index 8c9a1d4..86b3353 100644
--- a/tools/tampermonkey/README.md
+++ b/tools/tampermonkey/README.md
@@ -137,3 +137,23 @@ planning/repo-structure-memory.md
 ```
 
 Use it before planning/editing when the chat may have lost track of root areas, documentation layers, workstream files or app/tooling/example areas.
+
+
+## 9. Default Archive Review Diff File Mode
+
+Archive/package responses now use review-diff-file mode by default.
+
+Standard review files:
+
+```text
+_ai-review-diffs/last-archive.diff
+_ai-review-diffs/last-archive-summary.md
+```
+
+The archive apply command creates these files. A separate command commits and pushes only these review artifacts. Real archive files must not be committed or pushed before diff review.
+
+The chat review command is:
+
+```text
+проверь review diff file
+```
diff --git a/tools/tampermonkey/chat-command-palette.user.js b/tools/tampermonkey/chat-command-palette.user.js
index 4531298..7260c31 100644
--- a/tools/tampermonkey/chat-command-palette.user.js
+++ b/tools/tampermonkey/chat-command-palette.user.js
@@ -19,7 +19,7 @@
     "group": "MVP-1",
     "label": "давай архив",
     "description": "output package",
-    "body": "[ENMAN_COMMAND]\n\ncommand:\n  давай архив\n\ncommand_family:\n  `давай архив` / `собери архив` / `replacement package`\n\nsource_of_truth:\n  Start from `planning/planning-use-case-map.md`.\n  Then read the owner / linked files for this command route.\n\nroute_read_rule:\n  If you have not read this command route and its linked owner/example files in this chat, read them before answering.\n  If you have read them but do not remember the required behavior, boundaries or key points, reread from `planning/planning-use-case-map.md` before answering.\n  Do not rely only on this prompt when command behavior is uncertain.\n\nkey_reminders:\n  - Output-package mode, not archive read-source mode.\n  - Use active approved scope; ask only blocking questions.\n  - Do not put apply commands only inside the archive.\n  - Give apply/diff commands in chat.\n  - Save full diff to file and copy it to clipboard.\n  - Ask user to paste diff before commit.\n  - Do not commit or push.\n\nuser_target:\n  <what archive/package should include>\n\n[/ENMAN_COMMAND]"
+    "body": "[ENMAN_COMMAND]\n\ncommand:\n  давай архив\n\ncommand_family:\n  `давай архив` / `собери архив` / `давай архив с дифф-файлом` / `replacement package`\n\nsource_of_truth:\n  Start from `planning/planning-use-case-map.md`.\n  Then read the owner / linked files for this command route.\n\nroute_read_rule:\n  If you have not read this command route and its linked owner/example files in this chat, read them before answering.\n  If you have read them but do not remember the required behavior, boundaries or key points, reread from `planning/planning-use-case-map.md` before answering.\n  Do not rely only on this prompt when command behavior is uncertain.\n\nkey_reminders:\n  - Output-package mode, not archive read-source mode.\n  - Use active approved scope; ask only blocking questions.\n  - Do not put apply commands only inside the archive.\n  - Give apply commands and separate review-diff-only commit/push commands in chat.\n  - Default review uses `_ai-review-diffs/last-archive.diff`, not pasted clipboard diff.\n  - Commit/push only review diff files before review.\n  - Do not commit or push real archive files before diff review.\n\nuser_target:\n  <what archive/package should include>\n\n[/ENMAN_COMMAND]"
   },
   {
     "id": "archive_source.use",
