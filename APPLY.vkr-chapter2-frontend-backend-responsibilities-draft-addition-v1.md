# Apply — vkr-chapter2-frontend-backend-responsibilities-draft-addition-v1

Use the PowerShell command from the chat response.

The command:
1. extracts the archive to `%TEMP%`;
2. backs up an existing target file if present;
3. copies `planning/` and `_archive-review/`;
4. copies manifest/apply files;
5. runs `git status`.
