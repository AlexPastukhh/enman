# Raw Author Message Log

Status: archive-local raw notes / not normative / for thesis-writing voice extraction only

## Captured author messages/topics

- UI requirements should live in scenario UI specs, not only in client slice drafts.
- The proposed UI scenario structure is fully accepted and should be fixed.
- We need to inspect which UI scenarios are complete and which are incomplete.
- UI scenarios should be completed before updating client slice drafts or moving from the current UI to a normal implementation.
- The transition problem from the current UI to a slice-owned UI should be documented separately.
- The transition document is not an application scenario; it is a temporary implementation goal and may later be deleted or used for diploma writing.
- Raw author message logs must be local to the archive, not one shared file in the project, because archives can overwrite each other.
- The chat should preserve the user's raw formulations separately so they can later be cleaned up and used as more human diploma wording.
- Real rules derived from raw messages should live in normal visible docs, while raw messages should remain separate.
- Before creating an archive, list which author messages will be added to the raw log.
- My Applicant Parties UI is not actual as a separate UI target.
- Applicant Parties live inside the Account page section.
- All ApplicantParty actions should happen in the Account page section.
- Edit/delete/archive are not implemented now.
