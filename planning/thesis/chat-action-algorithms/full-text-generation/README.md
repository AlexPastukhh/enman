# Full text generation algorithms

This folder contains algorithms for connected VKR text generation.

Main file:

```text
vkr-full-text-version-generation.md
```

Full-text generation uses the same source-pass and source priority discipline as topic drafting, but outputs academic VKR text.
