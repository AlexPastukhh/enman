# General Project Info

## 1. Назначение проекта

Проект представляет собой web-приложение для выпускной квалификационной работы на тему:

**«Разработка web-приложения для автоматизации ведения документооборота и обработки клиентских заявок в сетевой компании ООО „ЗСК“»**

Основная цель проекта — разработать систему, которая позволяет клиентам подавать заявки через сайт, а сотрудникам организации — просматривать, проверять, одобрять или отклонять эти заявки, формировать обратную связь и вести связанный документооборот.

Приложение не реализует полноценный юридически значимый электронный документооборот. В рамках диплома система ограничивается приемом и обработкой заявок, формированием проекта договора/документа и отправкой обратной связи клиенту.

## 2. Технологический стек

### Backend

- ASP.NET Core;
- C#;
- Entity Framework Core;
- SQL Server;
- FluentValidation;
- MediatR;
- Serilog;
- Swagger/OpenAPI.

### Frontend

- React;
- TypeScript;
- Vite;
- React Router;
- React Query;
- React Hook Form;
- Zod;
- Vitest;
- Testing Library.

### Testing

- xUnit;
- FluentAssertions;
- Moq;
- Microsoft.AspNetCore.Mvc.Testing;
- Playwright.

## 3. Общая архитектура

```text
React / TypeScript frontend
        ↓ HTTP / REST
ASP.NET Core API
        ↓ EF Core
SQL Server database
```

Желательная логическая структура:

```text
Domain          — доменные сущности и бизнес-правила
Application     — use cases / commands / queries / services
Infrastructure  — EF Core, email, files, external/mock services
API             — controllers, DTO, HTTP contracts
Client          — React UI
Tests           — unit, integration, component, E2E tests
```

## 4. Главная бизнес-логика

Основной L1-процесс:

```text
Клиент регистрируется
→ входит в систему
→ создает заявителя
→ подает заявку
→ сотрудник видит заявку
→ сотрудник берет заявку в обработку
→ сотрудник вручную проверяет данные
→ сотрудник одобряет или отклоняет заявку
→ система формирует обратную связь
→ система отправляет email
→ при одобрении создается проект договора/документа
```

В первом слое сотрудник не выполняет автоматических проверок. Он вручную открывает заявку, проверяет данные и принимает решение.

## 5. Границы договора

В рамках диплома договорный контур ограничен проектом договора.

### L1

- создать простой проект договора;
- связать его с заявкой;
- отправить уведомление по email.

### L2

- шаблоны договоров;
- версии договора;
- PDF;
- подтверждение ознакомления;
- запрос корректировки.

### Не реализуется

- электронная подпись;
- юридически значимое подписание;
- платежи;
- реальный ЭДО;
- реальная СМЭВ-интеграция.

## 6. Основные акторы

| Актор | Слой | Назначение |
|---|---:|---|
| Гость | L1 | Неавторизованный пользователь |
| Клиент | L1 | Подает заявки |
| Сотрудник | L1 | Обрабатывает заявки |
| Администратор | L2/L3 | Управляет шаблонами, справочниками, пользователями |
| Email-сервис | L1 | Отправка уведомлений |
| Mock-сервис проверки | L2 | Имитация межведомственных проверок |
| Windows-домен | L3 | Корпоративная авторизация сотрудников |
| Фоновый обработчик | L3 | Outbox/retry уведомлений |
