# Scenario Diagrams Folder

Canonical location for generated scenario artifacts.

This update includes text/spec documentation only. Do not delete existing `.drawio`, `.png`, `.svg` files when applying it unless you intentionally regenerate diagrams.

Subfolders:

```text
scenario-text-specs/ = concrete textual scenario specs
scenario-data/ = concrete DATA specs
```
