# SC-15 — Security Text Specification

## Status
Text specification only.

Auth is not a separate scenario by default. It appears as preconditions, invariants and access rules in concrete scenarios.

Security topics: login throttling, rate limiting, account lock, no account enumeration, recovery token expiration/single-use, password recovery abuse protection, client own-resource access, employee permissions.
