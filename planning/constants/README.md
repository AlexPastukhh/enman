# Constants Planning Redirect

Status: superseded redirect  
Scope: pointer to the primary constants cross-cutting slice

The primary implementation-ready constants generation/testing document is now:

```text
planning/slices/cross-cutting/CC-CONST-001-client-constants-generation-and-contract-testing.md
```

Use `planning/constants/` only as a compatibility pointer if older docs referenced it.

Do not treat this folder as the source of truth for constants writer/checker implementation.
