# Scenario Responsibility Core

Status: superseded

Use instead:

```text
planning/tables/scenario-domain-design-input-core.md
```

Reason:

The broad layer-responsibility table is not the active bridge to domain modeling.

The active workflow now needs:

```text
scenario specs + DATA + server/domain validation
-> value object candidates
-> state-transition invariants
-> candidate domain methods
-> aggregate boundary pressure
-> domain discovery
```

See:

```text
planning/tables/README.md
planning/tables/scenario-domain-design-input-core.md
```
