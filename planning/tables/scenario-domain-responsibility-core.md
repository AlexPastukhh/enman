# Scenario Domain Responsibility Core

Status: superseded by scenario-domain-design-input-core.md

Use instead:

```text
planning/tables/scenario-domain-design-input-core.md
```

Reason:

The domain responsibility extraction was useful as a stepping stone, but the next planning need is more concrete:

```text
- value object candidates;
- server-side/domain validation rules;
- state/status transition invariants;
- candidate domain methods;
- aggregate boundary pressure.
```

These are now captured in:

```text
planning/tables/scenario-domain-design-input-core.md
```
