# Deprecated Client Slice Short Draft Rules

Status: deprecated / replaced

This file is kept only for old links.

Do not use this file for new client slice drafts.

Use the current canonical client template:

```text
planning/slices/client/CLIENT-SLICE-TEMPLATE.md
```

Use the current client drafting workflow:

```text
planning/slices/client/CLIENT-SLICE-DRAFTING-WORKFLOW.md
```

Use the current slice testing workflow:

```text
planning/slices/slice-test-plan-workflow.md
```

New client slice drafts must include:

```text
Scenario sources
Visual UI / Scenario Flow
Visual Layout / Screen Composition
Visual Client Implementation Flow
Styling / CSS Ownership
Validation / Feedback / Error UI
Accessibility / ARIA Contract
Behavior Coverage
Test / Verification Plan with Behavior-to-Test Trace
```

New client slice drafts should be created under:

```text
planning/slices/client/
```

Client-side cross-cutting slice drafts should be created under:

```text
planning/slices/client/cross-cutting/
```
