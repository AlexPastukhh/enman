Тема: Реализация сценариев приложения через slices

Статус: быстрый topic-драфт v1 / глава 3 / самый важный файл для преддипломной / нужен repo-check по фактическим slices, endpoints, handlers, UI routes, tests и demo flow.

1. Зачем тема нужна

Эта тема показывает, какие пользовательские сценарии реально реализованы в приложении.

Главная мысль:

Реализация приложения может быть описана через вертикальные slices: каждый slice закрывает конкретный пользовательский сценарий от UI и API до application/domain logic и хранения данных. Такой подход удобен для преддипломной, потому что позволяет быстро показать, что система не только спроектирована, но и работает по ключевой цепочке: заявка → рассмотрение → решение → договорный обмен → файл.

2. Что такое slice в этой главе

В этой главе slice — это не обязательно отдельная папка в коде. Это реализованный вертикальный сценарий.

Для каждого slice фиксируем:

1. Пользовательское действие.
2. Роль: клиент или сотрудник.
3. UI-экран.
4. API endpoint / handler.
5. Application/domain logic.
6. Хранение результата.
7. Тест или demo-проверка.

Тезис:

Slice показывает реализацию сценария целиком: от действия пользователя в интерфейсе до изменения состояния в backend и сохранения результата.

3. Основная цепочка slices

Минимальная цепочка для допуска:

1. Создание заявителя.
2. Создание заявки.
3. Просмотр заявок клиентом.
4. Просмотр заявок сотрудником.
5. Открытие карточки заявки.
6. Начало рассмотрения.
7. Проверка данных.
8. Одобрение заявки.
9. Отклонение заявки.
10. Начало договорного обмена.
11. Отправка версии договорного предложения.
12. Загрузка файла.
13. Просмотр истории версий.
14. Получение файла.

Сжатая demo-цепочка:

клиент создаёт заявку
→ сотрудник начинает рассмотрение
→ сотрудник одобряет заявку
→ начинается договорный обмен
→ отправляется версия предложения с файлом
→ клиент видит и скачивает файл
4. Таблица реализованных slices
Slice	Роль	Что делает пользователь	Что делает backend	Что сохраняется	Demo
Создание заявителя	клиент	заполняет данные заявителя	проверяет пользователя и DTO	заявитель в БД	создать заявителя
Создание заявки	клиент	отправляет форму заявки	создаёт заявку через command	заявка, статус, связь с заявителем	создать заявку
Список заявок клиента	клиент	открывает свои заявки	возвращает read DTO	ничего не меняет	показать my requests
Список заявок сотрудника	сотрудник	открывает рабочий список	возвращает employee read DTO	ничего не меняет	показать employee dashboard
Карточка заявки	клиент/сотрудник	смотрит состояние заявки	возвращает details DTO	ничего не меняет	открыть карточку
Начало рассмотрения	сотрудник	нажимает start review	проверяет роль и состояние	review / статус	начать review
Проверка данных	сотрудник	запускает проверку	выполняет mock/check-сценарий	результат проверки, если сохраняется	показать check result
Одобрение заявки	сотрудник	нажимает approve	применяет domain transition	решение Approved	одобрить заявку
Отклонение заявки	сотрудник	вводит причину и reject	проверяет reason и transition	решение Rejected + reason	отклонить заявку
Начало exchange	сотрудник	начинает договорный обмен	проверяет approved request	exchange	открыть exchange
Отправка версии	клиент/сотрудник	отправляет предложение	создаёт proposal version	версия предложения	показать history
Upload файла	клиент/сотрудник	выбирает файл	сохраняет file + metadata	physical file + metadata	загрузить файл
Download файла	клиент/сотрудник	нажимает скачать	проверяет metadata/access	не меняет состояние	скачать файл

Подпись:

Таблица 3.x — Реализованные slices пользовательских сценариев
5. Slice: создание заявителя

Что показать:

- клиент создаёт заявителя;
- заявитель связан с клиентской учётной записью;
- заявитель потом используется при создании заявки.

Текст:

Slice создания заявителя реализует первый шаг клиентского сценария. Пользователь вводит данные заявителя, frontend отправляет command на backend, backend проверяет контекст пользователя и сохраняет заявителя в БД.

Repo-check:

- как называется сущность заявителя;
- какие поля формы есть;
- какой endpoint/handler создаёт заявителя;
- есть ли тесты.
6. Slice: создание заявки

Что показать:

- клиент выбирает/использует заявителя;
- заполняет форму заявки;
- frontend отправляет command;
- backend создаёт заявку;
- статус устанавливается backend-ом.

Текст:

Slice создания заявки показывает основной клиентский сценарий. Frontend собирает данные формы, но не задаёт статус заявки напрямую. Backend создаёт заявку, связывает её с заявителем и сохраняет начальное состояние процесса.

Repo-check:

- fields create request form;
- create request endpoint;
- request entity/status;
- связь request → applicant;
- tests create request.
7. Slice: просмотр заявок

Клиентский список:

клиент видит только свои заявки.

Сотруднический список:

сотрудник видит заявки для обработки.

Текст:

Списки заявок реализованы как read slices. Они не изменяют состояние процесса, а возвращают DTO, удобные для отображения в интерфейсе.

Repo-check:

- client list endpoint;
- employee list endpoint;
- DTO fields;
- pagination/filter/sort, если есть.
8. Slice: карточка заявки

Карточка заявки — центральный slice.

Показывает:

данные заявки;
данные заявителя;
статус;
review state;
decision;
rejection reason;
available actions;
agreement exchange link, если есть.

Текст:

Карточка заявки объединяет данные нескольких частей процесса и показывает актуальное состояние заявки. Для клиента она является экраном отслеживания, а для сотрудника — рабочим экраном обработки.

Repo-check:

- request details endpoint;
- DTO карточки;
- availableActions;
- разные представления для клиента/сотрудника.
9. Slice: начало рассмотрения

Flow:

сотрудник открывает заявку
→ нажимает “Начать рассмотрение”
→ backend проверяет роль и состояние
→ создаётся/обновляется review state
→ UI обновляется

Текст:

Slice начала рассмотрения показывает сценарную природу command API. Frontend не меняет статус напрямую, а запускает команду, после которой backend проверяет допустимость перехода.

Repo-check:

- start review endpoint;
- domain/application rule;
- что сохраняется;
- что возвращается в command result.
10. Slice: проверка данных

Flow:

сотрудник запускает проверку
→ backend выполняет mock/check operation
→ результат отображается в карточке
→ решение всё равно принимает сотрудник

Текст:

Проверка данных реализуется как вспомогательный slice. Она помогает сотруднику принять решение, но не заменяет approve/reject и не меняет итог заявки автоматически.

Repo-check:

- run check endpoint;
- где хранится результат;
- как результат показывается в UI;
- tests/mock service.
11. Slice: одобрение заявки

Flow:

approve command
→ access check
→ state check
→ domain transition
→ save Approved
→ UI refresh

Текст:

Slice одобрения заявки реализует бизнес-переход заявки в положительное решение. Этот переход выполняется на backend после проверки роли сотрудника и текущего состояния заявки.

Repo-check:

- approve endpoint/handler;
- domain method/rule;
- status/decision fields;
- tests approve.
12. Slice: отклонение заявки

Flow:

reject command
→ reason validation
→ state check
→ domain transition
→ save Rejected + reason
→ client sees result

Текст:

Slice отклонения фиксирует отрицательное решение по заявке и причину отказа, если она предусмотрена моделью. Причина затем отображается клиенту в карточке заявки.

Repo-check:

- reject endpoint;
- обязательность reason;
- где хранится reason;
- tests reject.
13. Slice: начало договорного обмена

Flow:

approved request
→ сотрудник starts exchange
→ backend проверяет approved state
→ создаётся exchange
→ UI открывает договорный этап

Текст:

Договорный обмен начинается только после одобрения заявки. Этот slice связывает lifecycle заявки с последующим обменом версиями договорного предложения.

Repo-check:

- start exchange endpoint;
- exchange entity;
- связь exchange → request;
- UI route exchange.
14. Slice: отправка версии предложения

Flow:

пользователь выбирает действие отправки версии
→ frontend отправляет command
→ backend проверяет exchange state
→ создаёт proposal version
→ связывает sender
→ обновляет историю версий

Текст:

Slice отправки версии предложения реализует основной сценарий договорного обмена. Версия предложения является бизнес-объектом, а не просто загруженным файлом.

Repo-check:

- send proposal endpoint;
- proposal version entity;
- sender side;
- exchange history DTO.
15. Slice: загрузка файла

Flow:

frontend отправляет file
→ backend проверяет доступ
→ сохраняет physical file
→ создаёт document metadata
→ связывает metadata с proposal version

Текст:

Slice загрузки файла связывает API, storage и договорный обмен. Файл сохраняется отдельно, а в БД создаётся metadata-запись, связывающая physical file с версией предложения.

Repo-check:

- upload endpoint;
- file storage service;
- document metadata;
- originalFileName/storageKey;
- tests upload.
16. Slice: получение файла

Flow:

frontend запрашивает documentId
→ backend находит metadata
→ проверяет access через proposal/exchange/request
→ читает physical file
→ возвращает file или safe error

Текст:

Slice получения файла показывает, что договорные документы не раздаются как public static files. Backend проверяет доступ и только после этого возвращает physical file.

Repo-check:

- download endpoint;
- download by documentId или другой id;
- access check;
- no storage path exposure;
- tests forbidden download.
17. Тестирование slices

Для допуска можно использовать таблицу:

Slice	Unit tests	API/integration tests	Manual demo
Create request	если есть	если есть	да
Start review	если есть	если есть	да
Approve request	желательно	если есть	да
Reject request	желательно	если есть	да
Start exchange	если есть	если есть	да
Upload file	если есть	если есть	да
Download file	если есть	если есть	да
Forbidden access	если есть	если есть	желательно

Подпись:

Таблица 3.x — Проверка реализованных slices
18. Demo checklist по slices

Самый короткий сценарий для показа:

1. Создать заявку клиентом.
2. Открыть её в списке сотрудника.
3. Начать рассмотрение.
4. Запустить проверку данных.
5. Одобрить заявку.
6. Начать договорный обмен.
7. Загрузить файл версии предложения.
8. Открыть клиентом договорный обмен.
9. Скачать файл.

Если что-то сломается, запасной сценарий:

показать уже созданные seed/test данные
→ открыть заявку
→ показать review/decision
→ показать exchange/history/file metadata
→ показать тесты
19. Скриншоты для этого раздела
1. Форма создания заявки.
2. Список заявок клиента.
3. Список заявок сотрудника.
4. Карточка заявки до review.
5. Карточка заявки после start review.
6. Результат проверки данных.
7. Одобренная заявка.
8. Договорный exchange.
9. История версий.
10. Файл в версии предложения.
11. Download файла.
12. Запуск тестов.
20. Черновой текст

Реализация приложения может быть описана через набор вертикальных slices, каждый из которых соответствует пользовательскому сценарию. Такой slice включает frontend-экран, API-операцию, application/domain logic и сохранение результата.

Основная цепочка реализации начинается с создания заявителя и заявки клиентом. После этого сотрудник видит заявку в рабочем списке, открывает карточку, начинает рассмотрение, выполняет проверку данных и принимает решение. При одобрении заявки становится доступен договорный обмен, в рамках которого стороны могут отправлять версии договорного предложения и работать с прикреплёнными файлами.

Такой подход удобен для проверки реализации, потому что каждый slice можно отдельно продемонстрировать и протестировать. Например, slice одобрения заявки включает кнопку в интерфейсе сотрудника, command API, проверку состояния заявки в domain layer и сохранение решения в БД.

21. Repo-check
- реальные названия slices/features;
- реальные frontend routes;
- реальные endpoint names;
- реальные handler/use case names;
- реальные DTO;
- реальные entity/domain class names;
- какие slices покрыты тестами;
- какие slices можно показать на demo;
- есть ли seed/test data;
- какие сценарии сейчас нестабильны;
- что лучше не показывать на преддипломной.
22. Итоговая формула
глава 3 должна показывать реализацию через slices;

slice = пользовательский сценарий от UI до хранения;

главные slices:
создание заявителя,
создание заявки,
просмотр заявок,
карточка заявки,
start review,
check data,
approve/reject,
start exchange,
send proposal version,
upload file,
download file;

для допуска важнее не идеальное описание всех классов,
а доказательство,
что основная цепочка работает;

каждый slice нужно связать с:
UI,
API,
application/domain logic,
storage,
tests/demo.

Следующий быстрый файл:

planning/thesis/vkr-topic-workbench/04-chapter-3-implementation/
└─ 03-api-implementation.topic.md
Думал на протяжении 12s

Добавлять потом сюда:

planning/thesis/vkr-topic-workbench/
└─ 04-chapter-3-implementation/
   └─ 04-data-and-file-storage-implementation.topic.md

Полный путь:

planning/thesis/vkr-topic-workbench/04-chapter-3-implementation/04-data-and-file-storage-implementation.topic.md
Тема: Реализация хранения данных и файлов

Статус: быстрый topic-драфт v1 / глава 3 / нужен repo-check по БД, entities, migrations, metadata, file storage service, upload/download, access check, storage path/key, tests.

1. Зачем тема нужна

Эта тема показывает, как в реализации устроено хранение структурированных данных, metadata документов и физических файлов.

Главная мысль:

Реализация хранения разделяет состояние бизнес-процесса и физические файлы. БД хранит заявки, рассмотрения, решения, договорный обмен, версии предложений и metadata документов. Physical files хранятся отдельно, а backend связывает их с процессом через document metadata и контролирует доступ при upload/download.

2. Что нужно доказать

Для допуска нужно быстро показать:

1. Есть БД / persistence layer.
2. Есть сущности заявки, review, decision, exchange, proposal version.
3. Есть metadata документа.
4. Файл хранится отдельно от БД.
5. Metadata связывает proposal version и physical file.
6. Backend не отдаёт файл по прямому public path.
7. Download идёт через metadata и access check.
8. Есть хотя бы manual/API/demo-проверка upload/download.
3. Реализация хранения structured data

В БД должны сохраняться:

- пользователи / аккаунты / роли;
- заявители;
- заявки;
- статусы заявок;
- рассмотрение;
- результат проверки, если сохраняется;
- решение;
- причина отказа;
- договорный exchange;
- версии договорного предложения;
- отправитель версии;
- metadata документа.

Рабочий текст:

Структурированные данные процесса сохраняются в базе данных. Это позволяет backend восстанавливать состояние заявки, определять текущий этап жизненного цикла, проверять доступ пользователя и отображать актуальные данные во frontend.

4. Реализация document metadata

Metadata нужна для связи версии предложения с физическим файлом.

Возможные поля:

documentId;
proposalVersionId;
originalFileName;
storageKey / storageFileName;
contentType, если есть;
sizeBytes, если есть;
uploadedAt;
uploadedBy, если есть.

Осторожно:

точные поля проверить по repo;
не писать checksum/hash, если его нет;
не писать antivirus scan, если его нет;
не писать audit trail, если его нет.

Рабочий текст:

В БД хранится не сам файл, а metadata-запись. Она связывает физический файл с конкретной версией договорного предложения и содержит сведения, необходимые backend для поиска файла и отображения его во frontend.

5. Реализация физического хранения файлов

Physical file хранится отдельно.

Для текущего scope:

- файл сохраняется в файловой директории;
- backend формирует внутреннее имя или storage key;
- originalFileName используется для отображения;
- storageKey/path используется только backend;
- frontend не получает physical path.

Рабочий текст:

Физическое содержимое документа сохраняется в файловом хранилище. В рамках текущей реализации это может быть простая директория. При этом frontend не работает с файловой системой напрямую, а получает файл только через backend API.

6. Upload flow в реализации

Flow:

frontend отправляет файл
→ backend принимает upload request
→ backend проверяет пользователя и доступ к exchange
→ application/domain проверяет допустимость новой версии
→ file storage сохраняет physical file
→ persistence сохраняет metadata
→ proposal version связывается с document metadata
→ frontend получает result DTO

Рабочий текст:

При загрузке файла backend выполняет не только сохранение binary content, но и связывает файл с договорным обменом. Успешный upload должен создавать и physical file, и metadata, связанную с версией предложения.

7. Download flow в реализации

Flow:

frontend запрашивает documentId
→ backend находит metadata
→ backend определяет proposal version
→ backend определяет exchange и request
→ backend проверяет доступ пользователя
→ backend читает physical file по storage key
→ backend возвращает file или safe error

Рабочий текст:

Получение файла реализуется через backend. Сервер сначала получает metadata и проверяет бизнес-контекст документа, а затем читает physical file из хранилища. Это предотвращает прямой доступ к договорным файлам по пути файловой системы.

8. Metadata/file consistency

Риски:

metadata есть, file отсутствует;
file есть, metadata отсутствует;
upload завершился частично;
metadata указывает на устаревший storage key.

Проектный ответ:

успешный upload только после сохранения file + metadata;
file без metadata не считается документом процесса;
metadata без file возвращает safe file error;
сложный cleanup orphan files можно указать как future work.

Рабочий текст:

Так как metadata и physical file находятся в разных хранилищах, backend должен контролировать их согласованность на уровне сценариев upload/download.

9. Access check для файлов

Доступ проверяется по цепочке:

document metadata
→ proposal version
→ agreement exchange
→ connection request
→ applicant/client account

Тезис:

Пользователь не получает файл только по знанию имени или пути. Backend должен проверить, что document metadata относится к заявке или договорному обмену, доступному текущему пользователю.

10. Что не надо overclaim

Не писать без repo-check:

- используется полноценная СЭД;
- есть ЭДО;
- есть электронная подпись;
- есть object storage;
- есть signed URLs;
- есть antivirus scan;
- есть checksum/hash;
- есть полный аудит скачиваний;
- есть архивная политика;
- есть distributed transaction для file + metadata.

Писать безопасно:

- текущая модель достаточна для scope;
- object storage и ЭП/ЭДО — направления развития;
- физический файл хранится отдельно;
- metadata связывает файл с процессом;
- backend контролирует доступ.
11. Таблица для ВКР
Объект	Где хранится	Назначение
Заявитель	БД	данные клиента для подачи заявки
Заявка	БД	центральный объект процесса
Review	БД	состояние рассмотрения
Decision	БД	результат одобрения/отклонения
Rejection reason	БД	причина отказа
Agreement exchange	БД	договорный этап после одобрения
Proposal version	БД	версия договорного предложения
Document metadata	БД	связь версии с файлом
OriginalFileName	БД metadata	отображение пользователю
StorageKey/path	БД metadata / backend internal	поиск physical file
Physical file	file storage	содержимое документа

Подпись:

Таблица 3.x — Реализация хранения данных и файлов
12. Demo / evidence

Что показать на преддипломной:

1. БД с заявками / review / exchange / proposal.
2. Metadata документа в БД.
3. Физический файл в директории.
4. UI показывает originalFileName.
5. Download идёт через backend.
6. Storage path/key не виден во frontend.
7. Upload создаёт новую version/history row.
8. Ошибка missing/forbidden file возвращается safe.
13. Repo-check
- какая БД используется;
- где connection string / database config;
- где persistence layer;
- какие entity classes есть;
- ConnectionRequest;
- RequestReview;
- AgreementProposalExchange;
- AgreementProposal / ProposalVersion;
- AgreementDocumentRef / DocumentMetadata;
- поля metadata;
- originalFileName;
- storageKey/storagePath;
- где physical files реально сохраняются;
- есть ли file storage service;
- upload endpoint/service;
- download endpoint/service;
- access check при download;
- раскрывается ли storage path/key frontend;
- есть ли tests upload/download;
- есть ли tests metadata/file consistency;
- есть ли tests forbidden download.
14. Черновой текст

В реализации структурированные данные процесса сохраняются в базе данных. К ним относятся заявители, заявки, состояние рассмотрения, решения, договорные обмены и версии договорных предложений. Такое хранение позволяет backend восстанавливать актуальное состояние процесса и проверять доступ пользователя.

Файлы договорных предложений хранятся отдельно от структурированных данных. Для связи файла с процессом используется metadata-запись, которая сохраняется в БД и связывает physical file с конкретной версией договорного предложения. Metadata может содержать исходное имя файла, внутренний ключ хранения и дату загрузки.

Загрузка файла реализуется как командный сценарий. Backend проверяет доступ пользователя к договорному обмену, сохраняет physical file, создаёт metadata и связывает её с версией предложения. Получение файла также выполняется через backend: сервер сначала находит metadata, проверяет доступ через договорный обмен и заявку, а затем возвращает physical file.

Такой подход подтверждает проектное решение главы 2: domain logic не зависит от файловой системы, frontend не получает storage path, а доступ к договорным документам контролируется backend-ом.

15. Итоговая формула
реализация хранения разделяет:
structured data,
document metadata
и physical file;

БД хранит состояние процесса:
заявки,
review,
decision,
exchange,
proposal versions
и metadata;

файловое хранилище хранит physical file;

metadata связывает proposal version с файлом;

upload создаёт physical file + metadata;

download начинается с metadata
и проходит через access check;

frontend не получает storage path/storage key;

backend контролирует доступ
и consistency metadata/file;

эта реализация подтверждает storage boundary,
спроектированную в главе 2.

Следующий быстрый файл:

planning/thesis/vkr-topic-workbench/04-chapter-3-implementation/
└─ 05-frontend-implementation.topic.md
след
Думал на протяжении 10s

Добавлять потом сюда:

planning/thesis/vkr-topic-workbench/
└─ 04-chapter-3-implementation/
   └─ 05-frontend-implementation.topic.md

Полный путь:

planning/thesis/vkr-topic-workbench/04-chapter-3-implementation/05-frontend-implementation.topic.md
Тема: Реализация пользовательского интерфейса

Статус: быстрый topic-драфт v1 / глава 3 / нужен repo-check по routes, страницам, компонентам, forms, API client, guards, UI states, upload/download UI.

1. Зачем тема нужна

Эта тема показывает, как во frontend реализованы пользовательские сценарии клиента и сотрудника.

Главная мысль:

Frontend реализует интерфейс для двух ролей: клиента и сотрудника. Клиент создаёт заявителей и заявки, отслеживает состояние заявки и работает с договорным обменом. Сотрудник просматривает заявки, выполняет рассмотрение, принимает решение и сопровождает договорный этап. Все действия выполняются через backend API, а frontend не управляет бизнес-состоянием напрямую.

2. Что нужно доказать

Для допуска быстро показать:

1. Есть frontend-приложение.
2. Есть маршруты / страницы клиента.
3. Есть маршруты / страницы сотрудника.
4. Есть формы создания заявителя и заявки.
5. Есть список и карточка заявки.
6. Есть UI для review / approve / reject.
7. Есть UI договорного обмена.
8. Есть upload/download файлов.
9. Есть обработка loading / validation / business errors.
10. UI вызывает backend API, а не работает напрямую с БД/файлами.
3. Структура frontend

Frontend может быть описан через блоки:

routes / pages;
layouts;
forms;
lists;
details pages;
API client;
state management;
role guards;
UI components;
error/loading components;
file upload/download components.

Рабочий текст:

Frontend-часть приложения организована вокруг страниц и компонентов, соответствующих пользовательским сценариям. Страницы получают данные через read API, отправляют пользовательские действия через command API и отображают результат пользователю.

4. Клиентская зона

Клиентская зона реализует:

- личный кабинет клиента;
- список заявителей;
- создание заявителя;
- создание заявки;
- список моих заявок;
- карточку заявки;
- просмотр статуса;
- просмотр результата рассмотрения;
- просмотр причины отказа;
- переход к договорному обмену после одобрения;
- просмотр версий предложения;
- скачивание файла.

Рабочий текст:

Клиентский интерфейс реализует сценарий подачи заявки и отслеживания её состояния. Клиент видит только собственные данные и не получает доступ к действиям сотрудника.

5. Зона сотрудника

Сотрудническая зона реализует:

- рабочий список заявок;
- карточку заявки;
- блок рассмотрения;
- запуск проверки данных;
- одобрение заявки;
- отклонение заявки;
- ввод причины отказа;
- начало договорного обмена;
- отправку версии договорного предложения;
- загрузку файла;
- просмотр истории версий.

Рабочий текст:

Интерфейс сотрудника является рабочей зоной обработки заявок. Через него сотрудник открывает заявку, запускает review, выполняет проверку данных, принимает решение и переходит к договорному обмену.

6. Карточка заявки

Карточка заявки — основной экран frontend.

Она может показывать:

данные заявки;
данные заявителя;
текущий статус;
review state;
результат проверки;
decision;
rejection reason;
availableActions;
переход к agreement exchange;
file metadata, если договорный этап начат.

Для клиента:

экран отслеживания состояния.

Для сотрудника:

рабочий экран обработки.

Рабочий текст:

Карточка заявки объединяет данные процесса и показывает пользователю доступные действия. Для клиента это экран отслеживания, а для сотрудника — рабочий экран рассмотрения.

7. Формы frontend

Основные формы:

форма создания заявителя;
форма создания заявки;
форма причины отказа;
форма отправки версии предложения;
форма upload файла.

Frontend отвечает за:

сбор данных;
локальную проверку обязательных полей;
показ validation errors;
loading state;
disable кнопки при submit;
отправку command API;
показ результата.

Backend отвечает за:

окончательную validation;
проверку доступа;
business rules;
изменение состояния.

Рабочий текст:

Формы frontend помогают пользователю заполнить данные и отправить команду, но окончательная проверка и изменение состояния выполняются backend-ом.

8. AvailableActions и кнопки

Frontend может показывать кнопки на основе availableActions или похожих полей read DTO.

Примеры:

StartReview;
RunCheck;
Approve;
Reject;
StartExchange;
SendProposalVersion;
UploadFile;
DownloadFile.

Важно:

availableActions помогают UI;
кнопки можно скрывать или disable;
backend всё равно проверяет command повторно;
stale UI должен обрабатываться через business-state error.

Рабочий текст:

Доступные действия используются frontend для отображения кнопок, но не являются механизмом безопасности. Если состояние объекта изменилось, backend отклоняет команду и frontend обновляет экран.

9. UI договорного обмена

Экран договорного обмена показывает:

exchange status;
proposal versions;
sender side;
sentAt;
originalFileName;
documentId;
кнопку download;
форму upload/send version;
ошибки файла.

Не показывает:

storage path;
storage key;
server directory;
public uploads URL.

Рабочий текст:

В договорном обмене frontend отображает версии предложения и metadata документов. Physical file получается через download API, а не через прямую ссылку на файловую директорию.

10. UI states

Frontend должен показывать:

loading;
empty state;
success;
validation errors;
business-state errors;
forbidden / not found;
file unavailable;
unexpected error.

Примеры:

загрузка списка заявок;
пустой список;
ошибка формы;
действие уже недоступно;
файл недоступен;
нет доступа к заявке.

Рабочий текст:

Реализация frontend должна учитывать не только успешные сценарии, но и промежуточные и ошибочные состояния. Это особенно важно для command API, потому что read DTO может устареть.

11. Связь frontend с API

Frontend вызывает:

read API для списков и карточек;
command API для действий;
file upload API;
file download API.

Текст:

Frontend не содержит бизнес-правил lifecycle. Он отображает данные, полученные от backend, и отправляет пользовательские действия через API. Решение о допустимости действия остаётся на backend.

12. Таблица для ВКР
Экран / компонент	Роль	Назначение
Login / auth	клиент, сотрудник	вход и определение пользователя
Client dashboard	клиент	обзор клиентских данных
Applicants page	клиент	список и создание заявителей
Create request page	клиент	подача заявки
My requests page	клиент	список своих заявок
Employee requests page	сотрудник	рабочий список заявок
Request details page	клиент/сотрудник	карточка заявки
Review block	сотрудник	начало рассмотрения и проверка
Decision block	сотрудник	approve/reject
Agreement exchange page	клиент/сотрудник	договорный обмен
Proposal history	клиент/сотрудник	версии предложения
File controls	клиент/сотрудник	upload/download через API

Подпись:

Таблица 3.x — Реализованные frontend-экраны и компоненты
13. Скриншоты для допуска

Минимально собрать:

1. Login / главная.
2. Клиентский список заявок.
3. Форма создания заявки.
4. Карточка заявки клиента.
5. Список заявок сотрудника.
6. Карточка заявки сотрудника.
7. Review/check block.
8. Approve/reject.
9. Agreement exchange.
10. Proposal history.
11. Upload файла.
12. Download файла.
13. Validation error.
14. Business-state или forbidden/file error, если легко показать.
14. Repo-check
- frontend framework;
- routes/pages;
- layouts;
- client pages;
- employee pages;
- request form fields;
- applicants UI;
- request details UI;
- review/check UI;
- approve/reject UI;
- agreement exchange UI;
- proposal history UI;
- upload/download components;
- API client;
- availableActions usage;
- route guards;
- loading states;
- validation errors;
- business-state errors;
- file errors;
- storage path/key не показывается.
15. Черновой текст

Frontend-часть приложения реализует пользовательские сценарии клиента и сотрудника. Клиентская зона предназначена для создания заявителей и заявок, просмотра статуса заявки и работы с договорным обменом после одобрения. Сотрудническая зона предназначена для обработки заявок, выполнения проверки данных, принятия решения и сопровождения договорного этапа.

Основным экраном является карточка заявки. Она отображает текущее состояние процесса, данные заявки, результат рассмотрения и доступные действия. Для сотрудника карточка содержит действия обработки, а для клиента — информацию о ходе рассмотрения и результате.

Frontend взаимодействует с backend через API. Для получения данных используются read-запросы, а для изменения состояния — command-запросы. Frontend не устанавливает статус заявки самостоятельно: он отправляет команду, а backend проверяет доступ и бизнес-правила.

Отдельно реализуется работа с файлами договорного обмена. Интерфейс показывает имя файла и кнопку скачивания, но не раскрывает storage path или storage key. Получение файла выполняется через backend API.

16. Итоговая формула
frontend реализует пользовательские сценарии клиента и сотрудника;

клиент работает с заявителями,
заявками,
статусами
и договорным обменом;

сотрудник работает со списком заявок,
карточкой,
review,
decision
и договорным обменом;

карточка заявки — центральный экран;

frontend вызывает read API для данных
и command API для действий;

frontend не управляет lifecycle напрямую;

availableActions помогают показать кнопки,
но backend проверяет действие повторно;

UI показывает loading,
validation errors,
business-state errors
и file errors;

файлы отображаются через metadata,
а download идёт через backend API;

реализация frontend подтверждает UI-проектирование главы 2.

Последний быстрый файл для главы 3:

planning/thesis/vkr-topic-workbench/04-chapter-3-implementation/
└─ 06-testing-and-demo-readiness.topic.md
след 
Думал на протяжении 5s

Добавлять потом сюда:

planning/thesis/vkr-topic-workbench/
└─ 04-chapter-3-implementation/
   └─ 06-testing-and-demo-readiness.topic.md

Полный путь:

planning/thesis/vkr-topic-workbench/04-chapter-3-implementation/06-testing-and-demo-readiness.topic.md
Тема: Тестирование и готовность к демонстрации

Статус: быстрый topic-драфт v1 / глава 3 / закрывает доказательную часть для преддипломной / нужен repo-check по unit tests, API tests, frontend checks, manual demo, запуску проекта и стабильному demo-flow.

1. Зачем тема нужна

Эта тема показывает, как проверялась реализация и что можно продемонстрировать на преддипломной.

Главная мысль:

Для допуска важно показать не только наличие кода, но и проверяемость реализованных сценариев. Тестирование должно подтвердить основную цепочку приложения: создание заявки, рассмотрение, принятие решения, договорный обмен, загрузку и получение файла.

2. Что нужно доказать

Минимально для допуска:

1. Приложение запускается.
2. Backend и frontend связаны через API.
3. Основные сценарии проходят вручную.
4. Есть unit-тесты domain/application logic, если они есть.
5. Есть API/integration tests, если они есть.
6. Есть проверка upload/download файлов.
7. Есть demo-flow, который можно показать комиссии.
8. Есть screenshots / evidence.
3. Виды проверок
1. Unit tests
   Проверяют domain/application rules без UI и БД.

2. API / integration tests
   Проверяют endpoints, DTO, validation, access, persistence.

3. Frontend manual checks
   Проверяют страницы, формы, кнопки, ошибки.

4. File scenario checks
   Проверяют upload, metadata, download, safe errors.

5. End-to-end manual demo
   Проверяет всю пользовательскую цепочку.

Рабочий текст:

Проверка реализации выполняется на нескольких уровнях. Unit-тесты позволяют быстро проверить бизнес-правила, API-проверки подтверждают корректность взаимодействия frontend/backend, а ручной demo-flow показывает работоспособность приложения как целого.

4. Unit tests

Особенно важно, если есть выделенный domain layer.

Что тестировать:

- нельзя одобрить заявку в неправильном состоянии;
- нельзя отклонить заявку без review, если это правило есть;
- reason обязателен для reject, если это правило есть;
- после approve возможен договорный обмен;
- новый файл создаёт новую proposal version;
- повторное действие возвращает ошибку состояния.

Тезис:

Unit-тесты domain layer ценны тем, что проверяют бизнес-правила быстро и независимо от UI, БД и файловой системы.

5. API / integration tests

Что проверять:

- create applicant;
- create request;
- list requests;
- request details;
- start review;
- run data check;
- approve request;
- reject request;
- start exchange;
- send proposal version;
- upload file;
- download file;
- forbidden / not found;
- validation errors.

Тезис:

API-проверки подтверждают, что frontend может выполнять сценарии через backend-контракт, а backend корректно возвращает success, validation errors и business-state errors.

6. Manual scenario testing

Для преддипломной manual testing особенно полезен, потому что его можно быстро подтвердить скриншотами.

Основная цепочка:

1. Войти как клиент.
2. Создать заявителя.
3. Создать заявку.
4. Увидеть заявку в клиентском списке.
5. Войти как сотрудник.
6. Открыть список заявок.
7. Открыть карточку заявки.
8. Начать рассмотрение.
9. Выполнить проверку данных.
10. Одобрить заявку.
11. Начать договорный обмен.
12. Отправить версию предложения с файлом.
13. Войти как клиент.
14. Открыть договорный обмен.
15. Скачать файл.

Сжатый demo-flow:

создать заявку
→ start review
→ approve
→ start exchange
→ upload proposal file
→ download file
7. Проверка ошибок

Минимально проверить:

- пустая форма заявки → validation errors;
- reject без причины → validation error, если reason обязателен;
- повторный approve → business-state error;
- устаревшее действие → ActionNoLongerAvailable / аналог;
- чужая заявка → forbidden/not found;
- чужой файл → forbidden/not found;
- missing file → safe file error.

Тезис:

Проверка ошибок важна, потому что приложение должно корректно работать не только в успешном сценарии, но и при неверных данных, устаревшем состоянии или отсутствии доступа.

8. Проверка файлов

Проверить:

1. Upload создаёт physical file.
2. Upload создаёт metadata.
3. Metadata связана с proposal version.
4. UI показывает originalFileName.
5. Download идёт через backend.
6. Frontend не получает storage path/storage key.
7. Чужой пользователь не может скачать файл.
8. Missing file возвращает safe error.

Тезис:

Файловые сценарии подтверждают storage-boundary: physical file хранится отдельно, metadata связывает файл с договорным процессом, а download выполняется через backend и проверку доступа.

9. Таблица проверенных сценариев
Сценарий	Тип проверки	Ожидаемый результат
Создание заявителя	manual / API	заявитель появляется в системе
Создание заявки	manual / API	заявка создана и видна клиенту
Просмотр заявки сотрудником	manual / API	заявка видна в рабочем списке
Начало рассмотрения	manual / API / unit	заявка переходит в review
Проверка данных	manual / API	результат проверки отображается
Одобрение	manual / API / unit	заявка получает approved state
Отклонение	manual / API / unit	заявка получает rejected state + reason
Начало exchange	manual / API	создан договорный обмен
Отправка версии	manual / API	версия появляется в истории
Upload файла	manual / API	metadata + physical file созданы
Download файла	manual / API	файл скачивается через backend
Forbidden access	manual / API	доступ запрещён безопасно
Validation error	manual / API	ошибки отображаются в форме

Подпись:

Таблица 3.x — Проверенные сценарии реализации
10. Demo readiness checklist

Перед преддипломной проверить:

- backend запускается;
- frontend запускается;
- БД доступна;
- миграции применены, если нужны;
- есть тестовые пользователи client/employee;
- есть тестовые данные или можно быстро создать новые;
- upload directory доступна backend-у;
- файл можно загрузить;
- файл можно скачать;
- demo-flow проходит без ручного исправления БД;
- screenshots собраны;
- тесты запускаются одной командой или понятно где запускать.
11. Evidence package для допуска

Собрать в папку или документ:

1. Скрин структуры repo/solution.
2. Скрин запуска backend.
3. Скрин запуска frontend.
4. Скрин БД / миграций / таблиц.
5. Скрин клиентской формы заявки.
6. Скрин списка заявок сотрудника.
7. Скрин карточки заявки с review/actions.
8. Скрин approved request.
9. Скрин agreement exchange.
10. Скрин upload/download файла.
11. Скрин запуска тестов.
12. Таблица реализованных slices.
13. Таблица проверенных сценариев.
12. Что лучше не показывать, если не успеваем
- нестабильные edge cases;
- сложные ошибки доступа, если они не доделаны;
- неподтверждённые future work;
- ЭДО/ЭП/СЭД как будто реализованы;
- object storage, если используется простая директория;
- автогенерацию договора, если её нет;
- прямое редактирование БД во время демо.

Лучше показать стабильную короткую цепочку, чем пытаться показать всё.

13. Repo-check
- как запустить backend;
- как запустить frontend;
- как запустить tests;
- какие тесты реально есть;
- какие тесты проходят;
- какие сценарии покрыты unit tests;
- какие сценарии покрыты API/integration tests;
- есть ли seed users;
- есть ли seed data;
- есть ли demo files;
- где upload directory;
- какие сценарии нестабильны;
- какие screenshots нужно снять;
- какой demo-flow самый надёжный.
14. Черновой текст

Для проверки реализации использовались несколько уровней тестирования. Бизнес-правила, вынесенные в domain layer, могут проверяться быстрыми unit-тестами без запуска frontend, БД и файловой системы. Это позволяет убедиться, что ключевые переходы заявки и договорного обмена выполняются только в допустимых состояниях.

Кроме unit-тестов, важную роль играет проверка API-сценариев. Через backend API выполняются создание заявки, начало рассмотрения, одобрение, отклонение, начало договорного обмена, загрузка и получение файла. Такие проверки подтверждают, что frontend и backend взаимодействуют через согласованный контракт.

Для преддипломной также подготовлен демонстрационный сценарий, проходящий по основной цепочке приложения: клиент создаёт заявку, сотрудник рассматривает и одобряет её, затем начинается договорный обмен и отправляется версия договорного предложения с файлом. Этот сценарий показывает, что реализованы не отдельные экраны, а связанный процесс.

15. Итоговая формула
тестирование главы 3 должно доказать,
что реализованная система работает по основной цепочке;

unit tests проверяют business rules;

API/integration checks проверяют backend contract;

manual demo показывает работу приложения целиком;

главный demo-flow:
client creates request
→ employee reviews
→ employee approves
→ exchange starts
→ proposal file uploaded
→ client downloads file;

для допуска нужны:
таблица slices,
таблица проверенных сценариев,
скриншоты UI,
скрин запуска тестов
и короткий стабильный demo-flow.

Глава 3 быстрыми драфтами закрыта. Следующий практический шаг — не новый драфт, а сводка для преддипломной:

planning/thesis/vkr-topic-workbench/04-chapter-3-implementation/
└─ 00-predefense-demo-and-evidence-checklist.md
