# Merge risk report

Risk level: low-to-medium.

The archive replaces workflow markdown files only. It does not change source code or thesis text.

Main semantic change:

```text
existing chapters/subsections are treated as section draft candidates;
topic drafts receive reverse-engineered semantic points;
section drafts keep and revise candidate text.
```

Manual review recommended for:

- topic-card-template.md, because it changes the structure of future topic drafts;
- VKR-WORKFLOW-SOURCE-OF-TRUTH.md, because it is the main entry point;
- section-drafts/README.md, because it clarifies how existing text is reused.
