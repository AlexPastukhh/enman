# raw-author-message-log: vkr-chapter1-documents-feedback-drafts-replacement-v1

Relevant user decisions captured for these replacement drafts:

```text
Файлы реально загружаются в систему или хранится только ссылка/метаданные? — в БД SQL Server мета хранится, а в файловом хранилище — файл, но очень просто хранится тупо в директории, для диплома.

Какие метаданные документа реально есть? — смотреть по data-файлам из слоя сценариев, там актуально всегда всё.

Версия договора имеет отправителя? — да, отправитель версии один: клиент либо сотрудник. Со стороны организации процесс обмена могут обслуживать разные сотрудники, клиент в конкретном обмене один.

Терминология: будет “сотрудник”, не “работник”.

Для 1.2.3 нужна расширенная матрица вопросов, где “Зачем нужен” и “Текущее решение / предположение” не смешиваются.
```
