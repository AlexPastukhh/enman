# Raw Author Message Log

User request:

```text
давай начинай рефакторить, выдавай архив по всем правилам, за 1 архив рефактори сервер и соотв клиент драфт,юай пока не трогай, перед тем как делать архив пререпроверяй все ли так, ничго ли не убрано случайно и тд
```

Interpreted mode:

```text
Start the next docs-only refactor.
For one archive, refactor one server draft and the corresponding client draft.
Do not touch runtime UI/code.
Before archive release, recheck that nothing important was accidentally removed.
```
