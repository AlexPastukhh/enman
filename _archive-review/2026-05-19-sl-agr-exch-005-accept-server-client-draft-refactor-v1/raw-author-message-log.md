# Raw Author Message Log

```text
посмотри какой там не зарефактореный(параллельно с тобой могли зарефакторить) и делай след пару
```

Relevant standing instruction from uploaded prompt:

```text
Current mode: docs-only draft refactoring, one draft/slice or paired server/client draft per archive, implementation not rechecked, UI/page-flow/redirects out of scope, runtime code untouched, safe archive workflow required.
```
