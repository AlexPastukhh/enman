# Playwright locator cleanup v1.7

Incremental fix after v1.6.

Remaining failures were caused by `seed-e2e-demo-data` still failing with `Invalid column name 'ClientAccountId'`.

This archive does not touch domain, migrations, generated files, SMTP, production UI, or business handlers.
