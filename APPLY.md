# Apply — enman-applicant-template-per-type-scenario-sync-v1

Run from repository root:

```powershell
Expand-Archive -Path "C:\Users\alexa\Downloads\enman-applicant-template-per-type-scenario-sync-v1.zip" -DestinationPath . -Force
git status
```

Do not commit `MANIFEST.md` or `APPLY.md` unless explicitly wanted.
